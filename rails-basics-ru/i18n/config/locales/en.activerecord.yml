en:
  activerecord:
    models:
      # BEGIN
      post: Post
      # END
      post/comment: Comment
    attributes:
      # BEGIN
      post:  
        title: Title
        body: Body
      # END
      post/comment:
        body: Body

    errors:
      models:
        post:
          attributes:
            # BEGIN
            title:
              blank: not be blank
            body:
              blank: not be blank
            # END
